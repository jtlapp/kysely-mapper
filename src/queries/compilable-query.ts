/**
 * Interface tagging a query as compilable. When parameterizing a query,
 * the caller provides a function that must return a query having this tag.
 */
export interface CompilableMappingQuery {}
